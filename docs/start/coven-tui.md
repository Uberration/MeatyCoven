---
summary: "The prompt-first interactive menu launched by * Coven /command
Project sessions
Pick one. Enter casts.

Commands
1-9/a/x/q + Enter

> /start     Start here
  /tui       Open TUI
  /doctor    Doctor
  /daemon    Daemon sta…
  /run       Run an age…
  /patch     Patch Open…
  /sessions  Active ses…
  /all       All sessio…
  /attach    Attach ses…
  /summon    Summon ses…
  /archive   Archive se…
  /sacrifice Sacrifice …
  /quit      Quit

Preview
Setup check and a safe …
/start → coven doctor
Store: ~/.coven


Tip: run `coven doctor` first, then `coven run codex "fix the failing tests"`. or * Coven /command
Project sessions
Pick one. Enter casts.

Commands
1-9/a/x/q + Enter

> /start     Start here
  /tui       Open TUI
  /doctor    Doctor
  /daemon    Daemon sta…
  /run       Run an age…
  /patch     Patch Open…
  /sessions  Active ses…
  /all       All sessio…
  /attach    Attach ses…
  /summon    Summon ses…
  /archive   Archive se…
  /sacrifice Sacrifice …
  /quit      Quit

Preview
Setup check and a safe …
/start → coven doctor
Store: ~/.coven


Tip: run `coven doctor` first, then `coven run codex "fix the failing tests"`.."
read_when:
  - Browsing what the interactive Coven menu can do
title: "Coven TUI"
---

`coven` or `coven tui` opens the prompt-first interface. It accepts:

- Free-form task text (`fix the failing tests`).
- Slash commands (`/run codex <task>`, `/sessions`, `/archive`).
- Arrow-key navigation through ritual menus.

The TUI is the recommended starting point for new users.
